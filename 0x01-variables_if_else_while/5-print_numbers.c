#include <stdio.h>
/**
 * main - prints out single digits of numbers of base ten from zero to nine
 *
 * Return: 0 (success)
 */
int main(void)
{
	int i;
	for (i = 0; i <= 9; i++)
		printf("%d\n", i);
	return (0);
}
