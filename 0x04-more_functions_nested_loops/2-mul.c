#include "main.h"

/**
 * mul - multiplies two integers
 * @a: one of the integer involved
 * @b: the other integer involved
 *
 * Return: the multiplication result
 */
int mul(int a, int b)
{
	return (a * b);
}

