#include <stdlib.h>
#include <stdio.h>

/**
 * main - check code
 * @argc: counts the arguments no.
 * @argv: array to a pointer
 *
 * Return: 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	(void)argv;
	return (0);
}
