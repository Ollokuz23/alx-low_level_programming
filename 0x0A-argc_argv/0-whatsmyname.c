#include <stdlib.h>
#include <stdio.h>

/**
 * main - check code
 * @argc: counts the arguments
 * @argv: array to a pointer
 *
 * Return: 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
